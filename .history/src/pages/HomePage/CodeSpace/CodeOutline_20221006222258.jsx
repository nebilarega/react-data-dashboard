import { makeStyles } from "@material-ui/core";

const useStyles = makeStyles({
  codeOutline: {
    width: "30%",
    height: "100%",
  },
});

const CodeOutline = () => {
  return <div></div>;
};

export default CodeOutline;
