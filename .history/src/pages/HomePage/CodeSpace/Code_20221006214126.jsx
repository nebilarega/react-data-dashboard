import { makeStyles } from "@material-ui/core";

const useStyles = makeStyles({
  code: {
    display: "flex",
    flexDirection: "column",
  },
});
const Code = () => {
  return <div></div>;
};

export default Code;
